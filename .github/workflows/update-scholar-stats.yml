name: Update Google Scholar Stats

on:
  schedule:
    - cron: "0 0 * * 1"  # Every Monday at UTC 00:00
  workflow_dispatch:        # Allow manual trigger

permissions:
  contents: write

jobs:
  update-stats:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install scholarly "bibtexparser<2" "httpx<0.28"  # scholarly 1.7.11 breaks on bibtexparser 2.x and on httpx 0.28 (proxies kwarg removed)

      - name: Fetch and update scholar stats
        run: python scripts/update_scholar_stats.py

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git diff --quiet || (git add _pages/about.md && git commit -m "chore: update Google Scholar stats" && git push)
